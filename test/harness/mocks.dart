// Centralized `mocktail` mock declarations live here. The project currently
// has no service/repository boundaries to mock (Remote Config / Firebase were
// removed), so this file is intentionally empty. Add mocks here as new
// boundaries appear, then `setUpAll(registerFallbacks)` for any `any()`
// matchers that need fallback values.
